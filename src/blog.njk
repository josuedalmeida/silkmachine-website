---
layout: layouts/base.njk # Volte para este caminho mais simples
title: Blog da Silk Machine
permalink: /blog/index.html
---



<main id="main-content" role="main" class="section">
    <div class="container">
        <h1 class="section-title">Nosso Blog</h1>
        <p class="section-subtitle">Fique por dentro das novidades, dicas e tutoriais sobre serigrafia e as máquinas Silk Machine.</p>

        <div class="grid grid-2">
            {% for post in collections.posts | reverse %}
            <div class="card">
                <a href="{{ post.url }}" style="text-decoration: none; color: inherit;">
                    <h3>{{ post.data.title }}</h3>
                </a>
                <p style="font-size: 0.9em; color: var(--text-light); margin-bottom: 1rem;">
                    Por {{ post.data.author }} em {{ post.data.date | dateFormat("dd/MM/yyyy") }}
                </p>
                <p>{{ post.data.description or post.templateContent | striptags | truncate(150) }}</p>
                <p style="margin-top: 1rem;"><a href="{{ post.url }}" class="cta-button" style="padding: 0.5rem 1rem; font-size: 0.9em;">Leia Mais</a></p>
            </div>
            {% endfor %}
        </div>
    </div>
</main>
