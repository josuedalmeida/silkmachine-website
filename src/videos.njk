---
layout: layouts/base.njk
title: Vídeos - Silk Machine
permalink: /videos/index.html
---

<div class="videos-page">
    <!-- Header da página -->
    <div class="videos-header">
        <div class="container">
            <h1 class="videos-title">Central de Vídeos</h1>
            <p class="videos-subtitle">Descubra todo o potencial da K3-350 através dos nossos vídeos</p>
            
            <!-- Filtros e busca -->
            <div class="videos-controls">
                <div class="search-box">
                    <input type="text" id="video-search" placeholder="Buscar vídeos..." />
                    <i class="search-icon">🔍</i>
                </div>
                
                <div class="category-filters">
                    <button class="filter-btn active" data-category="all">Todos ({{ collections.videos.length }})</button>
                    <button class="filter-btn" data-category="demonstracoes">Demonstrações</button>
                    <button class="filter-btn" data-category="depoimentos">Depoimentos</button>
                    <button class="filter-btn" data-category="treinamentos">Treinamentos</button>
                    <button class="filter-btn" data-category="dicas-negocio">Dicas de Negócio</button>
                    <button class="filter-btn" data-category="tutoriais">Tutoriais</button>
                    <button class="filter-btn" data-category="novidades">Novidades</button>
                </div>
            </div>
        </div>
    </div>

    <!-- Banner de Destaque -->
    {% set featuredVideo = collections.videos | where("data.featured", true) | first %}
    {% if featuredVideo %}
    <section class="hero-banner">
        <div class="container">
            <div class="banner-content">
                <div class="banner-video">
                    <div class="video-thumbnail-large">
                        {% if featuredVideo.data.custom_thumbnail %}
                            <img src="{{ featuredVideo.data.custom_thumbnail }}" alt="{{ featuredVideo.data.title }}">
                        {% elif featuredVideo.data.platform == "youtube" or "youtube.com" in featuredVideo.data.video_url %}
                            {% set videoId = featuredVideo.data.video_url | replace("https://www.youtube.com/shorts/", "") | replace("https://www.youtube.com/watch?v=", "") | replace("https://youtu.be/", "") | replace("\\?.*", "") | replace("&.*", "") %}
                            <img src="https://img.youtube.com/vi/{{ videoId }}/maxresdefault.jpg" 
                                 alt="{{ featuredVideo.data.title }}"
                                 onerror="this.src='https://img.youtube.com/vi/{{ videoId }}/hqdefault.jpg'">
                        {% endif %}
                        <div class="play-overlay-large">
                            <div class="play-button-large">▶</div>
                        </div>
                        <div class="video-badge-large">⭐ Em Destaque</div>
                    </div>
                </div>
                <div class="banner-info">
                    <h2 class="banner-title">{{ featuredVideo.data.title }}</h2>
                    <p class="banner-description">{{ featuredVideo.data.description }}</p>
                    <div class="banner-meta">
                        <span class="banner-category">
                            {% if featuredVideo.data.category == "demonstracoes" %}🎬 Demonstrações
                            {% elif featuredVideo.data.category == "depoimentos" %}💬 Depoimentos
                            {% elif featuredVideo.data.category == "treinamentos" %}🎓 Treinamentos
                            {% elif featuredVideo.data.category == "dicas-negocio" %}💡 Dicas de Negócio
                            {% elif featuredVideo.data.category == "novidades" %}📢 Novidades
                            {% else %}{{ featuredVideo.data.category | replace("-", " ") | title }}
                            {% endif %}
                        </span>
                        {% if featuredVideo.data.duration %}
                        <span class="banner-duration">
                            {% if featuredVideo.data.duration >= 60 %}
                                {{ (featuredVideo.data.duration / 60) | round }}:{{ (featuredVideo.data.duration % 60) | string | padStart(2, "0") }}
                            {% else %}
                                {{ featuredVideo.data.duration }}s
                            {% endif %}
                        </span>
                        {% endif %}
                    </div>
                    <button class="watch-btn-large" onclick="openVideoModal('{{ featuredVideo.data.video_url }}', '{{ featuredVideo.data.title }}')">
                        <span class="btn-icon">▶</span>
                        Assistir Agora
                    </button>
                </div>
            </div>
        </div>
    </section>
    {% endif %}

    <!-- Carrosséis por Categoria -->
    <section class="video-carousels">
        <div class="container">
            
            <!-- Demonstrações -->
            {% set demonstracoes = collections.videos | where("data.category", "demonstracoes") %}
            {% if demonstracoes.length > 0 %}
            <div class="carousel-section">
                <h3 class="carousel-title">🎬 Demonstrações ({{ demonstracoes.length }})</h3>
                <div class="video-carousel" data-category="demonstracoes">
                    <div class="carousel-track">
                        {% for video in demonstracoes %}
                        {% include "partials/video-card-small.njk" %}
                        {% endfor %}
                    </div>
                    <button class="carousel-btn carousel-prev" aria-label="Anterior">‹</button>
                    <button class="carousel-btn carousel-next" aria-label="Próximo">›</button>
                </div>
            </div>
            {% endif %}

            <!-- Depoimentos -->
            {% set depoimentos = collections.videos | where("data.category", "depoimentos") %}
            {% if depoimentos.length > 0 %}
            <div class="carousel-section">
                <h3 class="carousel-title">💬 Depoimentos ({{ depoimentos.length }})</h3>
                <div class="video-carousel" data-category="depoimentos">
                    <div class="carousel-track">
                        {% for video in depoimentos %}
                        {% include "partials/video-card-small.njk" %}
                        {% endfor %}
                    </div>
                    <button class="carousel-btn carousel-prev" aria-label="Anterior">‹</button>
                    <button class="carousel-btn carousel-next" aria-label="Próximo">›</button>
                </div>
            </div>
            {% endif %}

            <!-- Dicas de Negócio -->
            {% set dicasNegocio = collections.videos | where("data.category", "dicas-negocio") %}
            {% if dicasNegocio.length > 0 %}
            <div class="carousel-section">
                <h3 class="carousel-title">💡 Dicas de Negócio ({{ dicasNegocio.length }})</h3>
                <div class="video-carousel" data-category="dicas-negocio">
                    <div class="carousel-track">
                        {% for video in dicasNegocio %}
                        {% include "partials/video-card-small.njk" %}
                        {% endfor %}
                    </div>
                    <button class="carousel-btn carousel-prev" aria-label="Anterior">‹</button>
                    <button class="carousel-btn carousel-next" aria-label="Próximo">›</button>
                </div>
            </div>
            {% endif %}

            <!-- Treinamentos -->
            {% set treinamentos = collections.videos | where("data.category", "treinamentos") %}
            {% if treinamentos.length > 0 %}
            <div class="carousel-section">
                <h3 class="carousel-title">🎓 Treinamentos ({{ treinamentos.length }})</h3>
                <div class="video-carousel" data-category="treinamentos">
                    <div class="carousel-track">
                        {% for video in treinamentos %}
                        {% include "partials/video-card-small.njk" %}
                        {% endfor %}
                    </div>
                    <button class="carousel-btn carousel-prev" aria-label="Anterior">‹</button>
                    <button class="carousel-btn carousel-next" aria-label="Próximo">›</button>
                </div>
            </div>
            {% endif %}

            <!-- Tutoriais -->
            {% set tutoriais = collections.videos | where("data.category", "tutoriais") %}
            {% if tutoriais.length > 0 %}
            <div class="carousel-section">
                <h3 class="carousel-title">📚 Tutoriais ({{ tutoriais.length }})</h3>
                <div class="video-carousel" data-category="tutoriais">
                    <div class="carousel-track">
                        {% for video in tutoriais %}
                        {% include "partials/video-card-small.njk" %}
                        {% endfor %}
                    </div>
                    <button class="carousel-btn carousel-prev" aria-label="Anterior">‹</button>
                    <button class="carousel-btn carousel-next" aria-label="Próximo">›</button>
                </div>
            </div>
            {% endif %}

            <!-- Novidades -->
            {% set novidades = collections.videos | where("data.category", "novidades") %}
            {% if novidades.length > 0 %}
            <div class="carousel-section">
                <h3 class="carousel-title">📢 Novidades ({{ novidades.length }})</h3>
                <div class="video-carousel" data-category="novidades">
                    <div class="carousel-track">
                        {% for video in novidades %}
                        {% include "partials/video-card-small.njk" %}
                        {% endfor %}
                    </div>
                    <button class="carousel-btn carousel-prev" aria-label="Anterior">‹</button>
                    <button class="carousel-btn carousel-next" aria-label="Próximo">›</button>
                </div>
            </div>
            {% endif %}

        </div>
    </section>

    <!-- Modal do player de vídeo -->
    <div id="video-modal" class="video-modal">
        <div class="modal-overlay"></div>
        <div class="modal-content">
            <div class="modal-header">
                <h3 class="modal-title"></h3>
                <button class="modal-close">&times;</button>
            </div>
            <div class="modal-body">
                <div id="video-player"></div>
            </div>
        </div>
    </div>

    <!-- Links para redes sociais -->
    <section class="social-links">
        <div class="container">
            <h2 class="section-title">Siga-nos nas Redes Sociais</h2>
            <div class="social-grid">
                <a href="https://www.youtube.com/@silkmachine/videos" target="_blank" class="social-card youtube">
                    <div class="social-icon">📺</div>
                    <h3>YouTube</h3>
                    <p>Vídeos completos e tutoriais detalhados</p>
                </a>
                <a href="https://www.instagram.com/silkmachine/" target="_blank" class="social-card instagram">
                    <div class="social-icon">📸</div>
                    <h3>Instagram</h3>
                    <p>Fotos e vídeos do dia a dia</p>
                </a>
                <a href="https://www.tiktok.com/@silkmachine" target="_blank" class="social-card tiktok">
                    <div class="social-icon">🎵</div>
                    <h3>TikTok</h3>
                    <p>Vídeos rápidos e criativos</p>
                </a>
                <a href="https://www.facebook.com/silkmachine" target="_blank" class="social-card facebook">
                    <div class="social-icon">👥</div>
                    <h3>Facebook</h3>
                    <p>Comunidade e atualizações</p>
                </a>
            </div>
        </div>
    </section>
</div>

